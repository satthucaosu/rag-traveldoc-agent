# GEMINI.md

**Audience: Antigravity** (always-loaded in this workspace). Keep this file **lean** — one screen or less.

Cross-tool rules, commands, and the skills index live in **`AGENTS.md`**. Load that file for stack, build/test commands, hard rules, and workflow.

## Antigravity-only notes

- **Skills:** workspace skills in `.agents/skills/`; invoke by name when the task matches (see `AGENTS.md` skills table).
- **MCP:** project servers in `.agents/mcp_config.json` (copy from `mcp/mcp_config.antigravity.example.json`); follow `mcp/CHECKLIST.md`.
- **Harness guide (humans):** `HARNESS.md` — not loaded every agent turn.

Add lines here only for rules that apply to **every** Antigravity prompt in this repo and do not belong in `AGENTS.md`.
